# Mirrors the previous bunfig.toml `linker = "hoisted"`: a flat node_modules.
# Required by electron-builder (clients/desktop), which does not support pnpm's
# default isolated layout. Switching to the isolated linker is a separate change.
node-linker=hoisted
